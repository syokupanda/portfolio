<h1>Recipes#index</h1>
<% @recipes.each do |recipe| %>
  <%= link_to recipe_path(recipe.id) do %>
    <%= attachment_image_tag(recipe, :image, size: "220x170", fallback: "no_image.jpg") %>
  <% end %>
  <%= recipe.name %>
  <%= recipe.introduction %>
  
<% end %>
